# Claude Code
CONTENTS.txt
CLAUDE-CODE-QUICK-REF.md
HANDOFF-COMPLETE-CONTEXT.md
README-WEEKEND-PLAN.md
SYSTEM-PROMPT-FOR-CLAUDE-CODE.md
.claude/

# Deploy configurations (keep local, may contain sensitive hostnames)
deploy.yml
deploy-*.yml
**/deploy.yml
**/deploy-*.yml

# ...except authored test fixtures, which are not real deploy configs and
# must be tracked so the specs that load them work in a fresh checkout.
!odysseus-core/spec/fixtures/deploy-*.yml
!odysseus-cli/spec/fixtures/deploy.yml

# Secrets
*.enc
secrets.yml
.env
.env.*

# Test directories with real configs
test-deploy/
test-ghost/

# Infrastructure
tofu/

# Tool versions (local preference)
.tool-versions

# RSpec example status tracking
**/spec/examples.txt

# Built gems
*.gem
