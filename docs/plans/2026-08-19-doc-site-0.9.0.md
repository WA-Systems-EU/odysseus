# Doc site to 0.9.0 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bring the documentation site from the 0.3.x feature set it currently describes up to what odysseus 0.9.0 actually does, starting with the pages that contradict the tool.

**Architecture:** Next.js App Router with Markdoc. Each page is `src/app/docs/<slug>/page.md` with YAML front-matter (`title:`) and a `{% .lead %}` first paragraph; the sidebar is a hand-maintained array in `src/lib/navigation.ts`. Renaming a page means moving a directory, updating that array, and fixing inbound links — Markdoc will not catch a broken `/docs/...` link, so the sweep is manual and is part of the work.

**Tech Stack:** Next.js, Markdoc (`{% callout %}` is the only custom tag in use), Tailwind. `npm run build` is the only check that exists — there are no tests.

**Source of truth:** the gem source at `/home/tsultrim/Code/WaConstellation/WaSystems/OdysseusProject/Odysseus` at version 0.9.0, and its `odysseus-cli/README.md`. Where a doc page and the code disagree, the code wins and the page changes.

**Repo:** `/home/tsultrim/Code/WaConstellation/WaSystems/OdysseusProject/Odysseus-doc-site`

## Global Constraints

- **The working tree has uncommitted changes** (`Gemfile`, `Gemfile.lock`, `src/app/docs/license/page.md`). Never run `git checkout`, `git stash`, `git reset` or `git clean`. `git add` only the files your task names.
- **Every factual claim must be checked against the gem source before it is written.** A page promising what the code does not do is the same defect as a README doing it. Cite the file you checked in your report.
- `accessories:` and `odysseus accessory` **still work** and print a deprecation notice. Pages must say the old spelling is accepted and deprecated — not pretend it never existed, and not present it as current.
- `odysseus setup` is **Ubuntu 24.04/26.04 only**, is for getting a single host going, and is not a provisioning tool: preparing servers at scale belongs to OpenTofu or equivalent. Every page that mentions it carries that framing.
- Deploys **never** gate on distro. Only `setup` does. Do not let the Ubuntu restriction leak into statements about deploying.
- `npm run build` must pass at the end of every task.
- Match the existing page voice: short lead sentence, `##` sections, fenced `shell`/`yaml` blocks, `{% callout %}` sparingly.

---

### Task 1: Rename Accessories → Dependencies (config page)

**Files:**
- Move: `src/app/docs/accessories/page.md` → `src/app/docs/dependencies/page.md`
- Modify: `src/lib/navigation.ts`
- Modify: `src/app/docs/configuration/page.md:320`
- Modify: `src/app/page.md:141`

**Interfaces:**
- Produces: the route `/docs/dependencies`, which Tasks 2 and 6 link to.

- [ ] **Step 1: Move the page and rewrite the key**

`git mv src/app/docs/accessories src/app/docs/dependencies`. In the moved page: front-matter `title: Dependencies`; every `accessories:` YAML key becomes `dependencies:`; every prose "accessory/accessories" becomes "dependency/dependencies"; the `## Managing accessories` heading becomes `## Managing dependencies`; every `odysseus accessory ...` command becomes `odysseus dependency ...`.

- [ ] **Step 2: Say the old spelling still works**

Immediately after the lead paragraph:

```markdown
{% callout title="Renamed from accessories" %}
`dependencies:` was called `accessories:` until 0.4.4, and `odysseus dependency` was `odysseus accessory`. Both old spellings still work and print a notice when used; they will be removed in a later release. Nothing on your hosts changes when you rename the key — container names and the `odysseus.service` label are built from the service name plus the dependency's own name, so running containers are adopted rather than orphaned.
{% /callout %}
```

Verify that claim against `odysseus-core/lib/odysseus/config/parser.rb` (both keys accepted, "Both present is an error") before writing it.

- [ ] **Step 3: Document removal, which is currently absent and is the part that bites**

Extend the `### Remove` section. Verified against `odysseus-core/lib/odysseus/orchestrator/dependency_deploy.rb:95-121`:

- `remove` finds its containers **through the dependency's config block**, so deleting the block from deploy.yml first strands the container: `remove` then answers `Dependency 'redis' not found in config` and nothing else will find it either. Remove first, then edit.
- `remove` stops and removes containers and **does not touch volumes** — a named volume and its data survive. Deleting data is a separate deliberate step on the host (`docker volume rm <name>`).
- `boot-all` boots what the config lists and **never removes what the config stopped listing**, deliberately: a typo or a half-merged branch would otherwise destroy a database.
- A container whose block is gone keeps running and does not appear in `dependency status`, which lists what the config declares rather than what the host runs (`dependency_manager.rb:44-56`).

- [ ] **Step 4: Update the nav and the two inbound links**

`src/lib/navigation.ts`: `{ title: 'Accessories', href: '/docs/accessories' }` → `{ title: 'Dependencies', href: '/docs/dependencies' }`.
`src/app/docs/configuration/page.md:320` and `src/app/page.md:141`: retitle and repoint.

- [ ] **Step 5: Build and commit**

```bash
npm run build
git add src/app/docs/dependencies src/lib/navigation.ts src/app/docs/configuration/page.md src/app/page.md
git commit -m "Rename the Accessories page to Dependencies"
```

Report any `/docs/accessories` reference the build did not catch — it will not catch any, which is why Task 6 exists.

---

### Task 2: Rename the accessory CLI page

**Files:**
- Move: `src/app/docs/cli-accessory/page.md` → `src/app/docs/cli-dependency/page.md`
- Modify: `src/lib/navigation.ts`

**Interfaces:**
- Consumes: `/docs/dependencies` from Task 1.
- Produces: the route `/docs/cli-dependency`.

- [ ] **Step 1: Move and rewrite**

`git mv`. Front-matter `title: dependency`. Every `odysseus accessory` → `odysseus dependency`; note `dep` is accepted as shorthand (verify in `odysseus-cli/bin/odysseus`). Its `## Configuration` section links to `/docs/dependencies`.

- [ ] **Step 2: Carry the same deprecation callout**

The one from Task 1 Step 2, trimmed to the command rename.

- [ ] **Step 3: Correct the host-argument split**

Verify against `odysseus-cli/README.md`'s `### dependency` section and fix the page to match: `boot`, `boot-all`, `remove`, `restart`, `upgrade` and `status` read hosts from the dependency's `hosts:` config and take **no** server argument; only `logs`, `exec` and `shell` take one.

- [ ] **Step 4: Nav, build, commit**

`{ title: 'accessory', href: '/docs/cli-accessory' }` → `{ title: 'dependency', href: '/docs/cli-dependency' }`, then `npm run build` and commit.

---

### Task 3: New page — `odysseus setup`

**Files:**
- Create: `src/app/docs/cli-setup/page.md`
- Modify: `src/lib/navigation.ts`

**Interfaces:**
- Produces: `/docs/cli-setup`, linked from Tasks 4 and 5.

- [ ] **Step 1: Read the code first**

`odysseus-cli/lib/odysseus/cli/setup_commands.rb`, `odysseus-core/lib/odysseus/setup/{escalation,public_key,preparer,docker_apt}.rb`, `odysseus-core/lib/odysseus/host_paths.rb`, and `odysseus-cli/README.md`'s `### setup`. Everything below must match what you read; where this plan and the code disagree, the code wins and you say so in your report.

- [ ] **Step 2: Write the page**

Front-matter `title: setup`. Lead: prepares a fresh host so odysseus can deploy to it as a non-root user.

Cover, each verified:

- What it does: creates the user `ssh.user` names, adds it to `docker`, installs the resolved public key by **appending**, creates the state directory, installs Docker if absent, then opens a **second SSH connection as the new user** and proves Docker and the state directory work from there. Success is reported only if that passes.
- `--as USER` — the identity it connects *as*, default `ubuntu`. `--as root` needs no sudo. Passwordless sudo is required for any other identity, because odysseus runs non-interactively and cannot answer a password prompt. If the default is wrong the error names the flag.
- `--key PATH` — repeatable; overrides the key source. A `--key` that resolves to no valid public key refuses, naming the path, rather than falling back.
- `ssh.user: root` is **refused**: setup exists to enable non-root deploys, root needs no preparation, and deploying as root already works.
- Reads **no new config keys** — user from `ssh.user`, hosts from `servers.*.hosts`, keys from `ssh.keys`.
- Docker install: from Docker's official apt repository; keyring and sources file replaced whole every run; apt runs non-interactively with a 300-second dpkg-lock wait and names the holder on timeout; **no repair of an apt state setup did not create**; the GPG fingerprint is deliberately unpinned. Success is `docker info` answering *after* the install, not apt exiting zero.
- Reports `:changed` distinctly from `:ok`; a second run against a prepared host issues no apt command at all.
- Ubuntu 24.04/26.04 only, refused by name otherwise — unlike `doctor`, which only warns, because a deploy needs a working daemon and does not care which distro provides it.
- A `{% callout %}` making the positioning explicit: this is for getting one host going; servers at scale belong to OpenTofu, Terraform or equivalent, and setup opens no ports and touches no firewall, swap, `sshd_config` or unattended-upgrades.

- [ ] **Step 3: Nav, build, commit**

Add `{ title: 'setup', href: '/docs/cli-setup' }` to the CLI Reference group, first, before `deploy`.

---

### Task 4: New page — `odysseus doctor`

**Files:**
- Create: `src/app/docs/cli-doctor/page.md`
- Modify: `src/lib/navigation.ts`

**Interfaces:**
- Consumes: `/docs/cli-setup` from Task 3.

- [ ] **Step 1: Read `odysseus-core/lib/odysseus/host_verifier.rb` and the README's `### doctor`**

- [ ] **Step 2: Write the page**

Front-matter `title: doctor`. Read-only diagnosis of every host, connecting as `ssh.user` — the deploy identity, not a bootstrap one. The five checks in order (`distro`, `docker`, `docker_group`, `state_dir`, `deploy_log`), what each proves, and that an unsupported distro **warns rather than fails**, because deploys work anywhere Docker does.

Say plainly that it is worth running however the host was prepared — including one a provisioning tool built — and that it is the acceptance test for `setup`. Link to `/docs/cli-setup`.

- [ ] **Step 3: Nav, build, commit** — after `setup` in the CLI Reference group.

---

### Task 5: Installation page — stop teaching a manual Docker install

**Files:**
- Modify: `src/app/docs/installation/page.md`

- [ ] **Step 1: Requirements**

`### Target servers` currently lists Docker and SSH. Rewrite: SSH access always; Docker **or** a supported Ubuntu and `odysseus setup`, which installs it. Add that deploys never check the distro — any host with a working Docker daemon will do.

- [ ] **Step 2: Replace the `## Server setup` section's manual install**

It currently teaches `curl -fsSL https://get.docker.com | sh` and `ssh-copy-id root@...`. Lead with `odysseus setup` instead, linked to `/docs/cli-setup`, then `odysseus doctor` to verify. Keep the manual route below it, explicitly for hosts that are not Ubuntu or that you provision yourself — do not delete it, it is still the honest answer for a tofu-built host.

Keep the `### Caddy (automatic)` subsection unchanged; it is still accurate.

- [ ] **Step 3: Fix the "Minimal server setup" callout (line 26)**

"Odysseus only requires Docker on your servers" is now half the story. Say Docker and SSH, that `setup` can install Docker on Ubuntu, and keep the true Caddy sentence.

- [ ] **Step 4: Build and commit**

---

### Task 6: Link sweep and dead-route check

**Files:**
- Modify: whichever pages the sweep finds.

- [ ] **Step 1: Find every stale reference**

```bash
grep -rn "accessor" src/ --include=*.md --include=*.ts
grep -rn "/docs/accessories\|/docs/cli-accessory" src/
```

Markdoc does not validate internal links, so a moved page leaves working-looking links that 404. Every hit must be updated or, where it is describing the deprecated spelling on purpose, left with a comment saying so.

- [ ] **Step 2: Verify every nav href resolves to a directory**

For each `href: '/docs/<slug>'` in `src/lib/navigation.ts`, confirm `src/app/docs/<slug>/page.md` exists. Report any that do not.

- [ ] **Step 3: Build, then commit**

```bash
npm run build
git add -u src
git commit -m "Repoint links left behind by the dependency rename"
```

---

---

### Task 7: New page — `odysseus rollback`

**Files:**
- Create: `src/app/docs/cli-rollback/page.md`
- Modify: `src/lib/navigation.ts`

**Interfaces:**
- Consumes: nothing. Produces: `/docs/cli-rollback`.

This is the command people reach for when a deploy has just gone wrong, so it
is the one where missing documentation costs most, at the worst moment.

- [ ] **Step 1: Read the code**

`odysseus-core/lib/odysseus/rollback_plan.rb`, `rollback_planner.rb`,
`host_versions.rb`, `deploy_log.rb`, the `rollback` method in
`odysseus-cli/lib/odysseus/cli/rollback_commands.rb`, and the README's
`### rollback`. Confirm what `--list` prints and where the candidate versions
come from before writing a word about either.

- [ ] **Step 2: Write the page**

Front-matter `title: rollback`. Cover:

- `odysseus rollback [VERSION]` returns **every role on every host** to a
  previously deployed version; with no VERSION it picks the previous one.
- `--list` shows the candidates per host and **changes nothing** — say plainly
  that this is the safe first move when you are not sure what to roll back to.
- Where candidates come from: the deploy log on each host, which is why
  `doctor` checks the deploy log's location as one of its five checks.
- What a version can and cannot tell you, and why it depends on how the deploy
  was versioned: a deploy from a clean git repository records the commit and
  the deployer, so `--list` can say what code a version was and who shipped it;
  a deploy named with `--image TAG` records the deployer but NOT the commit --
  `version_resolver.rb:27` sets `ref: nil` deliberately, and its comment says why
  ("deployer is available regardless, so there is no such reason to withhold
  it") -- and a reused tag leaves two entries odysseus cannot tell apart. Link to whichever page covers
  version naming. This is the strongest practical argument for the git-commit
  route and belongs here, where the cost actually lands.
- That hosts can disagree — a host that missed a deploy has a different history
  — and how the output shows that.

- [ ] **Step 3: Nav, build, commit**

Add `{ title: 'rollback', href: '/docs/cli-rollback' }` to CLI Reference,
directly after `deploy`.

---

### Task 8: Global options and `version`

**Files:**
- Create: `src/app/docs/cli-global/page.md`
- Modify: `src/lib/navigation.ts`

**Interfaces:**
- Consumes: nothing. Produces: `/docs/cli-global`, which Task 9 links to from
  the pages that currently repeat this material.

- [ ] **Step 1: Read `odysseus-cli/bin/odysseus`**

Specifically the `--debug` handling near the top, `print_version`, and the
`OptionParser` block. `--debug` is read before the command dispatch and also
honours `ODYSSEUS_DEBUG=1`.

- [ ] **Step 2: Write the page**

Front-matter `title: Global options`. Cover:

- `--config FILE` — path to deploy.yml, default `deploy.yml` in the working
  directory.
- `--debug` / `ODYSSEUS_DEBUG=1` — prints every command sent to the host and
  the connection as it opens. Say why it is worth reaching for first: it shows
  the literal shell line that ran, which is usually where the answer is. As of
  0.9.0 it reaches the SSH layer for every command, not only the UI.
- `-v` / `--verbose` — accepted by `deploy`, `build`, `pussh` and `setup`, and
  means the same as `--debug` for those. Do not claim it works everywhere;
  check the option parser and say exactly where it is read.
- `odysseus version` (and `--version`) — prints odysseus, odysseus-core and
  ruby versions. Show the real output shape.

- [ ] **Step 3: Nav, build, commit**

Add to the CLI Reference group, last.

---

### Task 9: Accuracy pass over the CLI pages that already exist

**Files:**
- Modify: `src/app/docs/cli-deploy/page.md`
- Modify: `src/app/docs/cli-build/page.md`
- Modify: `src/app/docs/cli-app/page.md`
- Modify: `src/app/docs/cli-secrets/page.md`

Documenting every command is not only about pages that are missing. These four
were written for 0.3.x and have not been checked since.

- [ ] **Step 1: Check each page's claims against the gem, one page at a time**

For each: read the corresponding section of `odysseus-cli/README.md` and the
command's implementation, then fix anything that disagrees. Report per page
what you found, including "nothing" where that is the answer.

Known to need attention, but do not treat this list as exhaustive:

- **`cli-deploy`'s `--image TAG` section.** It presents `--image` as an ordinary
  option. 0.9.0's framing is that a deploy from a clean git repository is the
  proper route — the tag then provably identifies the code, and the commit and
  deployer are recorded — while `--image` is the quick way that records the
  deployer but no commit, and lets a tag be reused. Do not write "records
  neither": `version_resolver.rb:27` keeps the deployer on that path on purpose. Rewrite it to match, and link to the rollback page
  from Task 7, where the cost is felt.
- **`cli-app`'s `--verbose, -v` section** and **`cli-deploy`'s**, against the
  new global-options page: replace duplicated explanation with a link, keeping
  only what is specific to that command.
- **`cli-app`'s `logs` section**: verify against the README's `### logs`, which
  documents behaviour that may post-date the page — stopped containers are
  included, the notice goes to stderr so redirection captures only logs, and
  finding no container at all exits non-zero.

- [ ] **Step 2: Build and commit, one commit per page**

Separate commits so a wrong correction can be reverted without taking the
others with it.

## Deliberately not in this plan

These are real gaps and belong in a follow-up, not here:

- **Sail plugins / `plugins:`** — zero coverage.
- **The non-root deploy user and `HostPaths`** — the whole 0.8.0 user model, including where state lives for root versus anyone else.
- **Rolling deploys** and **image retention**.
- A redirect from `/docs/accessories` to `/docs/dependencies`. Worth doing if the old URLs are already public; needs a decision about `next.config.mjs` redirects that is out of scope here.

## Definition of done

- **Every command in `odysseus --help` has a documented home**, verified by
  listing the help output and naming the page that covers each entry. The
  audit that produced Tasks 7-9: `cli-app` already covers `status`,
  `containers`, `logs`, `cleanup` and `validate` as well as the app family, so
  the only commands with no page at all were `rollback` and `version`.
- `npm run build` passes.
- `grep -rn "accessor" src/` returns only deliberate mentions of the deprecated spelling.
- Every `href` in `src/lib/navigation.ts` resolves to a page that exists.
- No page claims setup is available on anything but Ubuntu 24.04/26.04, and no page claims deploys require Ubuntu.
- The uncommitted `Gemfile`, `Gemfile.lock` and `license/page.md` changes are still uncommitted and unmodified.
