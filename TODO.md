# TODO list

## features

- ensure support of pussh for docker images
- add support for build image locally and on "build host" (kamal inspired)
- add static website to present odysseus (use https://tailwindcss.com/plus/templates/syntax as base template)
- start web UI project (hanami, github auth, list services, list available images, list currently deploy version, add storage for deploy logs)
- add CI config (buildkite)
- add support for secrets sourced from: OpenBao (self-hosted secrets management)
- add support for secrets sourced from: AWS Secrets Manager
